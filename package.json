{
	"private": true,
	"scripts": {
		"postinstall": "lerna bootstrap --hoist && lerna run link",
		"dev": "lerna run --parallel dev",
		"production": "lerna run --parallel production"
	},
	"devDependencies": {
		"lerna": "^2.7.1"
	},
	"dependencies": {}
}